// Interfaces

export interface Destino {
  direccion: string;
  numero: number;
}

export interface LiDestino {
    direccion: string;
    numero: string;
    calle: string;
    ciudad: string;
    lat: number;
    lng: number;
    id: string;
    infoWindow: boolean;
    opacidad: number;
}

